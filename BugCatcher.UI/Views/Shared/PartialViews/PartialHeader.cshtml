
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">BugCatcher</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Settings">Settings</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Query" asp-action="QueryList">Queries</a>
                </li>
                @if (User.IsInRole("Admin"))
                {
                <li>
                    <div class="dropdown show">
                        <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Admin
                        </a>

                        <div class="dropdown-menu" aria-labelledby="dropdownAdminButton">
                            <a class="dropdown-item" asp-controller="Item" asp-action="Create">New Item</a>
                            <a class="dropdown-item" asp-controller="Admin" asp-action="NewUser">New User</a>
                            <a class="dropdown-item" asp-controller="Admin" asp-action="UserList">Users</a>
                        </div>
                    </div>
                </li>
                }
                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Admin" asp-action="LogOut">Logout</a>
                    </li>
                }
            </ul>
        </div>
    </nav>
</header>